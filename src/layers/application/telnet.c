// Global libraries
#include <stdio.h>

// Local header files
#include "telnet.h"

int telnet_handler(const u_char *packet) {
    if (packet) {
        ;
    }
    printf("No handling yet\n");
    return 0;
}